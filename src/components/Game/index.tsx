import { GameConent } from "./GameContent";
import { GameCard } from "./GameCard";
import { GameAction } from "./GameAction";

export const GameC = {
  Content: GameConent,
  Card: GameCard,
  Action: GameAction,
}